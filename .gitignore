
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
data/
server

# Stray beads export at repo root (canonical file lives in .beads/issues.jsonl)
/issues.jsonl

# Node is build-time only (make assets); the bundled output is committed
node_modules/
